---
title: "Homework 2 draft"
author: "Ishrat Chowdhury"
date: "September 22, 2019"
output: html_document
---
 
```{r}
?ppois
ppois(7, lambda=15, lower.tail = FALSE) 
ppois(0, lambda = 15)
dpois(x=3, lambda = 15)
```

```{r}
library(ggplot2)
library(dplyr)
```
 
```{r}
plot(0:30, dpois( x=0:30, lambda=15 ))
?plot
hist(dpois(x=0:30, lambda = 15))
```

```{r}
rpois(104, 15)
x<-rpois(104, 15)
x
```

```{r}
?hist
hist(x, xlim = c(0, 30))
```

